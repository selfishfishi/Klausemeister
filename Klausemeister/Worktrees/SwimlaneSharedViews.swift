import SwiftUI

/// Corner radius shared across the swimlane panel's glass container and
/// drop-target overlays so the surfaces stay visually coherent if the
/// radius is tuned.
let swimlaneGlassCornerRadius: CGFloat = 10
